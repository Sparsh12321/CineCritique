<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/page.css">
</head>
<body>
    <div class="content">
   <h2><%= post.name %></h2>
   <img src="<%= post.image %>" alt="<%= post.name %>">
   <h4>Reviews</h4>
   <% for( review of post.reviews ) { %>
    <div class="reviews">
    <h5><%= review.username %></h5>
    <p><%= review.content %></p>
    <a href="http://localhost:8080/posts/<%=post.id %>/reviews/<%=review.id %>/edit">Edit Your review</a>
</div>
   <% } %>
   <br><br><br><br>
   <a href="http://localhost:8080/posts/<%=post.id %>/reviews/new">Add Your review</a>

</div>
</body>
</html>